
This directory was created by copying the examples/js directory from the
three.js(r73) distribution, and copying the three.js and three.min.js from
the three.js/build directory.

Then additional packages for this project were added to this js directory
at the top level.

